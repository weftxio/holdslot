# Base URL of the HoldSlot API.
# Local dev (api running via uvicorn): http://127.0.0.1:8000  (this is also the built-in default)
# Dev cloud (custom domain):          https://api.tryholdslot.com
# Dev cloud (raw API Gateway):        https://ooqe40p813.execute-api.us-east-1.amazonaws.com
# Copy to .env.local and adjust as needed.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
