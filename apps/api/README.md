# apps/api — placeholder

FastAPI backend (stages S0–S7, core spine, integrations, webhooks, worker). **Not built
yet** — the mock UI (`apps/web`) is the first development item. Inner structure is added at
its build-order step; see the product spec §2 for the planned layout.

Python service, managed outside the pnpm JS workspace.
